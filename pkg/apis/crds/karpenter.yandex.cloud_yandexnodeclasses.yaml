---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: yandexnodeclasses.karpenter.yandex.cloud
spec:
  group: karpenter.yandex.cloud
  names:
    kind: YandexNodeClass
    listKind: YandexNodeClassList
    plural: yandexnodeclasses
    singular: yandexnodeclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: YandexNodeClass is the Schema for the YandexNodeClass API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of YandexNodeClass
            properties:
              can_be_preemptible:
                default: false
                description: |-
                  CanBePreemptible determines if the nodes can be preemptible
                  By default, nodes are not preemptible
                type: boolean
              core_fractions:
                description: |-
                  CoreFractions is the list of core fractions to use for the nodes
                  If not specified, the default core fraction of 100% will be used
                items:
                  description: CoreFraction is a string representation of a core fraction
                  enum:
                  - 5
                  - 20
                  - 50
                  - 100
                  type: string
                type: array
              diskSize:
                anyOf:
                - type: integer
                - type: string
                default: 30Gi
                description: DiskSize is the size of the booted disk
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              diskType:
                default: network-ssd
                description: |-
                  DiskType is the type of disk to create
                  Valid values are:
                  - "network-hdd"
                  - "network-ssd" (default)
                  - "network-ssd-nonreplicated"
                enum:
                - network-hdd
                - network-ssd
                - network-ssd-nonreplicated
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels to apply to the VMs
                type: object
              nodeLabels:
                additionalProperties:
                  type: string
                description: NodeLabels is additional labels on node
                type: object
              platform:
                default: standard-v3
                description: |-
                  Platform is the platform of the nodes
                  Default is "standard-v3"
                enum:
                - standard-v1
                - standard-v2
                - standard-v3
                type: string
              securityGroups:
                description: SecurityGroups to apply to the VMs
                items:
                  type: string
                type: array
              subnetSelectorTerms:
                description: SubnetSelectorTerms is a list of subnet selector terms.
                  The terms are ORed.
                items:
                  description: |-
                    SubnetSelectorTerm defines selection logic for a subnet used by Karpenter to launch nodes.
                    If multiple fields are used for selection, the requirements are ANDed.
                  properties:
                    id:
                      description: ID is the subnet id in Yandex Cloud
                      pattern: subnet-[0-9a-z]+
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      description: |-
                        Tags is a map of key/value tags used to select subnets
                        Specifying '*' for a value selects all values for a given tag key.
                      maxProperties: 20
                      type: object
                      x-kubernetes-validations:
                      - message: empty label keys or values aren't supported
                        rule: self.all(k, k != '' && self[k] != '')
                  type: object
                maxItems: 30
                type: array
                x-kubernetes-validations:
                - message: subnetSelectorTerms cannot be empty
                  rule: self.size() != 0
                - message: expected at least one, got none, ['labels', 'id']
                  rule: self.all(x, has(x.labels) || has(x.id))
                - message: '''id'' is mutually exclusive, cannot be set with a combination
                    of other fields in a subnet selector term'
                  rule: '!self.all(x, has(x.id) && has(x.labels))'
            required:
            - subnetSelectorTerms
            type: object
          status:
            description: Status defines the observed state of YandexNodeClass
            properties:
              conditions:
                description: Conditions contains signals for health and readiness
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastValidationTime:
                description: LastValidationTime is the last time the nodeclass was
                  validated
                format: date-time
                type: string
              selectedInstanceTypes:
                description: |-
                  SelectedInstanceTypes contains the list of instance types that meet the requirements
                  Only populated when using automatic instance type selection
                items:
                  type: string
                type: array
              specHash:
                description: SpecHash is a hash of the YandexNodeClass spec
                format: int64
                type: integer
              subnets:
                description: |-
                  Subnets contains the current subnet values that are available to the
                  cluster under the subnet selectors.
                items:
                  description: Subnet contains resolved Subnet selector values utilized
                    for node launch
                  properties:
                    id:
                      description: ID of the subnet
                      type: string
                    zoneID:
                      description: The associated availability zone ID
                      type: string
                  required:
                  - id
                  type: object
                type: array
              validationError:
                description: ValidationError contains the error message from the last
                  validation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
